version: "3.8"

services:
  app:
    # container_name: name
    # image: name
    build: ./
    volumes:
      - ./:/app
      - /app/node_modules/
    ports:
      - "3000:3000"
    expose:
      - "3000"